<UserControl x:Class="MaxChemical.Modules.Designer.Views.DataAnalysisControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:oxy="http://oxyplot.org/wpf"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d">

    <UserControl.Resources>
        <!-- 现代化配色 -->
        <Color x:Key="PrimaryColor">#0078D4</Color>
        <Color x:Key="PrimaryDarkColor">#106EBE</Color>
        <Color x:Key="PrimaryLightColor">#E6F3FF</Color>
        <Color x:Key="SecondaryColor">#2B2B2B</Color>
        <Color x:Key="SuccessColor">#16C60C</Color>
        <Color x:Key="WarningColor">#FF8C00</Color>
        <Color x:Key="DangerColor">#E74856</Color>
        <Color x:Key="DarkColor">#1F1F1F</Color>
        <Color x:Key="GrayColor">#8A8A8A</Color>
        <Color x:Key="LightGrayColor">#F8F9FA</Color>
        <Color x:Key="WhiteColor">#FFFFFF</Color>

        <Color x:Key="WindowBackgroundColor">#FFFFFF</Color>
        <Color x:Key="ControlBackgroundColor">#FAFBFC</Color>
        <Color x:Key="BorderColor">#E1E5E9</Color>
        <Color x:Key="TextColor">#2B2B2B</Color>
        <Color x:Key="SecondaryTextColor">#666666</Color>
        <Color x:Key="AccentColor">#0078D4</Color>
        <Color x:Key="HoverColor">#F3F2F1</Color>
        <Color x:Key="SelectedColor">#E3F2FD</Color>

        <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}"/>
        <SolidColorBrush x:Key="PrimaryDarkBrush" Color="{StaticResource PrimaryDarkColor}"/>
        <SolidColorBrush x:Key="PrimaryLightBrush" Color="{StaticResource PrimaryLightColor}"/>
        <SolidColorBrush x:Key="SecondaryBrush" Color="{StaticResource SecondaryColor}"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessColor}"/>
        <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource WarningColor}"/>
        <SolidColorBrush x:Key="DangerBrush" Color="{StaticResource DangerColor}"/>
        <SolidColorBrush x:Key="DarkBrush" Color="{StaticResource DarkColor}"/>
        <SolidColorBrush x:Key="GrayBrush" Color="{StaticResource GrayColor}"/>
        <SolidColorBrush x:Key="LightGrayBrush" Color="{StaticResource LightGrayColor}"/>
        <SolidColorBrush x:Key="WhiteBrush" Color="{StaticResource WhiteColor}"/>
        <SolidColorBrush x:Key="WindowBackgroundBrush" Color="{StaticResource WindowBackgroundColor}"/>
        <SolidColorBrush x:Key="ControlBackgroundBrush" Color="{StaticResource ControlBackgroundColor}"/>
        <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}"/>
        <SolidColorBrush x:Key="TextBrush" Color="{StaticResource TextColor}"/>
        <SolidColorBrush x:Key="SecondaryTextBrush" Color="{StaticResource SecondaryTextColor}"/>
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="HoverBrush" Color="{StaticResource HoverColor}"/>
        <SolidColorBrush x:Key="SelectedBrush" Color="{StaticResource SelectedColor}"/>

        <!-- 现代化按钮样式 -->
        <Style x:Key="BaseButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                               CornerRadius="6" 
                               BorderThickness="{TemplateBinding BorderThickness}"
                               BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource HoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{StaticResource SelectedBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 主要按钮样式 -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <!-- 输入框样式 -->
        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="28"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                               BorderBrush="{TemplateBinding BorderBrush}"
                               BorderThickness="{TemplateBinding BorderThickness}"
                               CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                         Margin="{TemplateBinding Padding}"
                                         VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                <Setter Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 卡片样式 -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Direction="270" ShadowDepth="1" BlurRadius="3" Opacity="0.08"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 紧凑卡片样式 -->
        <Style x:Key="CompactCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Direction="270" ShadowDepth="1" BlurRadius="2" Opacity="0.05"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 滚动条样式 -->
        <Style x:Key="ThinScrollBarStyle" TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Width" Value="8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="{TemplateBinding Background}">
                            <Track x:Name="PART_Track" IsDirectionReversed="True" Focusable="False">
                                <Track.Thumb>
                                    <Thumb Background="#C0C0C0" Width="6">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border Background="{TemplateBinding Background}" 
                                                       CornerRadius="3"/>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#808080"/>
                                                    </Trigger>
                                                    <Trigger Property="IsDragging" Value="True">
                                                        <Setter Property="Background" Value="#606060"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageDownCommand" Opacity="0" Focusable="False"/>
                                </Track.IncreaseRepeatButton>
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageUpCommand" Opacity="0" Focusable="False"/>
                                </Track.DecreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ThinScrollViewerStyle" TargetType="ScrollViewer">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Grid Background="{TemplateBinding Background}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <ScrollContentPresenter Grid.Column="0" Grid.Row="0"
                                                   Content="{TemplateBinding Content}"
                                                   ContentTemplate="{TemplateBinding ContentTemplate}"
                                                   CanContentScroll="{TemplateBinding CanContentScroll}"/>

                            <ScrollBar Grid.Column="1" Grid.Row="0"
                                      Style="{StaticResource ThinScrollBarStyle}"
                                      Orientation="Vertical"
                                      Value="{TemplateBinding VerticalOffset}"
                                      Maximum="{TemplateBinding ScrollableHeight}"
                                      ViewportSize="{TemplateBinding ViewportHeight}"
                                      Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 图标几何 -->
        <Geometry x:Key="RefreshGeometry">M17.65,6.35C16.2,4.9 14.21,4 12,4A8,8 0 0,0 4,12A8,8 0 0,0 12,20C15.73,20 18.84,17.45 19.73,14H17.65C16.83,16.33 14.61,18 12,18A6,6 0 0,1 6,12A6,6 0 0,1 12,6C13.66,6 15.14,6.69 16.22,7.78L13,11H20V4L17.65,6.35Z</Geometry>
        <Geometry x:Key="ExportGeometry">M23,12L19,8V11H10V13H19V16M1,18V6C1,4.89 1.9,4 3,4H15A2,2 0 0,1 17,6V9H15V6H3V18H15V15H17V18A2,2 0 0,1 15,20H3A2,2 0 0,1 1,18Z</Geometry>
    </UserControl.Resources>

    <Grid Background="{StaticResource LightGrayBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260" MinWidth="240" MaxWidth="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 左侧参数输入面板 -->
        <Border Grid.Row="0" Grid.Column="0" 
                Background="White" 
                BorderBrush="{StaticResource BorderBrush}" 
                BorderThickness="0,0,1,0">
            <ScrollViewer Padding="12" 
                         VerticalScrollBarVisibility="Auto"   
                         Style="{StaticResource ThinScrollViewerStyle}">
                <StackPanel>
                    <!-- 标题 -->
                    <TextBlock Text="{Binding ConditionTitle}" 
                              FontSize="14" 
                              FontWeight="SemiBold" 
                              Foreground="{StaticResource TextBrush}"
                              Margin="10,15,0,16"/>

                    <!-- 反应条件卡片 -->
                    <Border Style="{StaticResource CardStyle}" Margin="0,0,0,12">
                        <StackPanel>
                            <TextBlock Text="{Binding ParamTitle}" 
                                      FontSize="14" 
                                      FontWeight="SemiBold" 
                                      Foreground="{StaticResource TextBrush}"
                                      Margin="0,0,0,10"/>

                            <!-- 压力 -->
                            <Grid Margin="0,0,0,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="55"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="40"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding PressureLabel}" VerticalAlignment="Center" FontSize="12" Foreground="{StaticResource TextBrush}"/>
                                <TextBox Grid.Column="1" 
                                        Text="{Binding Pressure, UpdateSourceTrigger=PropertyChanged}" 
                                        Style="{StaticResource ModernTextBoxStyle}" 
                                        Margin="4,0"/>
                                <TextBlock Grid.Column="2" Text="bar" VerticalAlignment="Center" FontSize="11" Foreground="{StaticResource SecondaryTextBrush}" Margin="4,0,0,0"/>
                            </Grid>

                            <!-- 温度 -->
                            <Grid Margin="0,0,0,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="55"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="40"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding TempratureLabel}" VerticalAlignment="Center" FontSize="12" Foreground="{StaticResource TextBrush}"/>
                                <TextBox Grid.Column="1" 
                                        Text="{Binding Temperature, UpdateSourceTrigger=PropertyChanged}" 
                                        Style="{StaticResource ModernTextBoxStyle}" 
                                        Margin="4,0"/>
                                <TextBlock Grid.Column="2" Text="°C" VerticalAlignment="Center" FontSize="11" Foreground="{StaticResource SecondaryTextBrush}" Margin="4,0,0,0"/>
                            </Grid>

                            <!-- H₂流量 -->
                            <Grid Margin="0,0,0,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="55"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="50"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding FlowLabel}" VerticalAlignment="Center" FontSize="12" Foreground="{StaticResource TextBrush}"/>
                                <TextBox Grid.Column="1" 
                                        Text="{Binding H2Flow, UpdateSourceTrigger=PropertyChanged}" 
                                        Style="{StaticResource ModernTextBoxStyle}" 
                                        Margin="4,0"/>
                                <TextBlock Grid.Column="2" Text="mL/min" VerticalAlignment="Center" FontSize="10" Foreground="{StaticResource SecondaryTextBrush}" Margin="4,0,0,0"/>
                            </Grid>

                            <!-- 原料流量 -->
                            <Grid Margin="0,0,0,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="55"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="50"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding IngredientsLabel}" VerticalAlignment="Center" FontSize="12" Foreground="{StaticResource TextBrush}"/>
                                <TextBox Grid.Column="1" 
                                        Text="{Binding FeedFlow, UpdateSourceTrigger=PropertyChanged}" 
                                        Style="{StaticResource ModernTextBoxStyle}" 
                                        Margin="4,0"/>
                                <TextBlock Grid.Column="2" Text="g/min" VerticalAlignment="Center" FontSize="10" Foreground="{StaticResource SecondaryTextBrush}" Margin="4,0,0,0"/>
                            </Grid>

                            <!-- 催化剂用量 -->
                            <Grid Margin="0,0,0,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="55"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="40"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding CatalystLabel}" VerticalAlignment="Center" FontSize="12" Foreground="{StaticResource TextBrush}"/>
                                <TextBox Grid.Column="1" 
                                        Text="{Binding CatAmount, UpdateSourceTrigger=PropertyChanged}" 
                                        Style="{StaticResource ModernTextBoxStyle}" 
                                        Margin="4,0"/>
                                <TextBlock Grid.Column="2" Text="%" VerticalAlignment="Center" FontSize="11" Foreground="{StaticResource SecondaryTextBrush}" Margin="4,0,0,0"/>
                            </Grid>

                            <!-- 停留时间 -->
                            <Grid Margin="0,0,0,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="55"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="40"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding StayLabel}" VerticalAlignment="Center" FontSize="12" Foreground="{StaticResource TextBrush}"/>
                                <TextBox Grid.Column="1" 
                                        Text="{Binding ResidenceTime, UpdateSourceTrigger=PropertyChanged}" 
                                        Style="{StaticResource ModernTextBoxStyle}" 
                                        Margin="4,0"/>
                                <TextBlock Grid.Column="2" Text="min" VerticalAlignment="Center" FontSize="11" Foreground="{StaticResource SecondaryTextBrush}" Margin="4,0,0,0"/>
                            </Grid>

                            <!-- H₂当量 -->
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="55"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="40"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding EquivalentLabel}" VerticalAlignment="Center" FontSize="12" Foreground="{StaticResource TextBrush}"/>
                                <TextBox Grid.Column="1" 
                                        Text="{Binding H2Equiv, UpdateSourceTrigger=PropertyChanged}" 
                                        Style="{StaticResource ModernTextBoxStyle}" 
                                        Margin="4,0"/>
                                <TextBlock Grid.Column="2" Text="eq" VerticalAlignment="Center" FontSize="11" Foreground="{StaticResource SecondaryTextBrush}" Margin="4,0,0,0"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- 预测按钮 -->
                    <Button Content="{Binding PredictiveAnalysisLabel}" 
                           Style="{StaticResource PrimaryButtonStyle}"
                           Command="{Binding PredictCommand}"
                           Height="36"
                           Margin="5,0,5,12"/>

                    <!-- 预测结果区域 -->
                    <StackPanel Visibility="{Binding PredictionResultsVisibility}">
                        <!-- 主要预测结果 -->
                        <Border CornerRadius="8"
                               Padding="16"
                               Margin="2,0,0,12">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#E6F3FF" Offset="0"/>
                                    <GradientStop Color="#CCE8FF" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.Effect>
                                <DropShadowEffect Color="#0078D4" Direction="270" ShadowDepth="2" BlurRadius="8" Opacity="0.15"/>
                            </Border.Effect>

                            <StackPanel>
                                <TextBlock Text="{Binding PredictiveResult}" 
                                          FontSize="14" 
                                          FontWeight="SemiBold" 
                                          Foreground="{StaticResource PrimaryDarkBrush}"
                                          Margin="0,0,0,8"/>

                                <TextBlock Text="{Binding PredictionResultText}" 
                                          FontSize="18" 
                                          FontWeight="Bold" 
                                          Foreground="{StaticResource PrimaryDarkBrush}"
                                          HorizontalAlignment="Center"
                                          Margin="0,0,0,6"/>

                                <TextBlock Text="{Binding ConfidenceIntervalText}" 
                                          FontSize="11" 
                                          Foreground="{StaticResource SecondaryTextBrush}"
                                          TextWrapping="Wrap"
                                          HorizontalAlignment="Center"
                                          TextAlignment="Center"/>
                            </StackPanel>
                        </Border>

                        <!-- 统计信息 -->
                        <Border Style="{StaticResource CompactCardStyle}" Margin="0,0,0,8">
                            <StackPanel>
                                <TextBlock Text="{Binding DetailStatisticLabel}" 
                                          FontSize="12" 
                                          FontWeight="SemiBold" 
                                          Foreground="{StaticResource TextBrush}"
                                          Margin="0,0,0,6"/>

                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0" 
                                              Text="{Binding ConversionRateLabel}" 
                                              Foreground="{StaticResource SecondaryTextBrush}" 
                                              FontSize="10" Margin="0,0,0,2"/>
                                    <TextBlock Grid.Row="0" Grid.Column="1" 
                                              Text="{Binding PredictedConversion}" 
                                              Foreground="{StaticResource TextBrush}" 
                                              FontSize="10" FontWeight="Medium" Margin="0,0,0,2"/>

                                    <TextBlock Grid.Row="1" Grid.Column="0" 
                                              Text="{Binding ConfidenceIntervalLabel}" 
                                              Foreground="{StaticResource SecondaryTextBrush}" 
                                              FontSize="10" Margin="0,0,0,2"/>
                                    <TextBlock Grid.Row="1" Grid.Column="1" 
                                              Text="{Binding ConfidenceRange}" 
                                              Foreground="{StaticResource TextBrush}" 
                                              FontSize="10" FontWeight="Medium" Margin="0,0,0,2"/>

                                    <TextBlock Grid.Row="2" Grid.Column="0" 
                                              Text="{Binding PredictionAccuracyLabel}" 
                                              Foreground="{StaticResource SecondaryTextBrush}" 
                                              FontSize="10" Margin="0,0,0,2"/>
                                    <TextBlock Grid.Row="2" Grid.Column="1" 
                                              Text="{Binding PredictionAccuracy}" 
                                              Foreground="{StaticResource TextBrush}" 
                                              FontSize="10" FontWeight="Medium" Margin="0,0,0,2"/>

                                    <TextBlock Grid.Row="3" Grid.Column="0" 
                                              Text="{Binding ProcessRiskLabel}" 
                                              Foreground="{StaticResource SecondaryTextBrush}" 
                                              FontSize="10"/>
                                    <TextBlock Grid.Row="3" Grid.Column="1" 
                                              Text="{Binding ProcessRisk}" 
                                              Foreground="{StaticResource TextBrush}" 
                                              FontSize="10" FontWeight="Medium"/>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <!-- 工艺建议 -->
                        <Border Style="{StaticResource CompactCardStyle}" Margin="0,0,0,8">
                            <StackPanel>
                                <TextBlock Text="{Binding ProcessSuggestionsLabel}" 
                                          FontSize="12" 
                                          FontWeight="SemiBold" 
                                          Foreground="{StaticResource TextBrush}"
                                          Margin="0,0,0,6"/>

                                <ItemsControl ItemsSource="{Binding Recommendations}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Text}" 
                                                      FontSize="10"
                                                      Foreground="{StaticResource TextBrush}"
                                                      TextWrapping="Wrap"
                                                      Margin="0,2"/>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <!-- 模型状态 -->
                    <Border Style="{StaticResource CompactCardStyle}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" 
                                      Text="{Binding ModelStatusLabel,StringFormat='{}{0}:'}" 
                                      Foreground="{StaticResource SecondaryTextBrush}" 
                                      FontSize="11" 
                                      VerticalAlignment="Center"
                                      Margin="0,0,8,0"/>
                            <TextBlock Grid.Column="1" 
                                      Text="{Binding ModelStatus}" 
                                      Foreground="{Binding ModelStatusBrush}" 
                                      FontSize="11" 
                                      FontWeight="Medium"
                                      VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- 主内容区域 -->
        <Grid Grid.Row="0" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 标题工具栏 -->
            <!-- 标题工具栏 -->
            <Border Grid.Row="0" 
       Background="{StaticResource WindowBackgroundBrush}" 
       BorderBrush="{StaticResource BorderBrush}" 
       BorderThickness="0,0,0,1"
       Padding="24,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- 左侧反应场景选择 -->
                    <StackPanel Grid.Column="0" 
                   Orientation="Horizontal"
                   VerticalAlignment="Center">
                        <TextBlock Text="{Binding ReactionBgLabel,StringFormat='{}{0}:'}" 
                      VerticalAlignment="Center"
                      FontSize="13"
                      Foreground="{StaticResource SecondaryTextBrush}"
                      Margin="0,0,10,0"/>

                        <ComboBox Width="200"
                     Height="32"
                     FontSize="13"
                     SelectedItem="{Binding SelectedReactionScenario, Mode=TwoWay}"
                     ItemsSource="{Binding ReactionScenarios}"
                     DisplayMemberPath="Name">
                            <ComboBox.Style>
                                <Style TargetType="ComboBox">
                                    <Setter Property="Background" Value="White"/>
                                    <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                                    <Setter Property="BorderThickness" Value="1"/>
                                    <Setter Property="Padding" Value="10,0"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ComboBox">
                                                <Grid>
                                                    <ToggleButton x:Name="ToggleButton"
                                                     Grid.Column="2"
                                                     Focusable="False"
                                                     IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                     ClickMode="Press">
                                                        <ToggleButton.Template>
                                                            <ControlTemplate TargetType="ToggleButton">
                                                                <Border x:Name="Border"
                                                           Background="{TemplateBinding Background}"
                                                           BorderBrush="{TemplateBinding BorderBrush}"
                                                           BorderThickness="{TemplateBinding BorderThickness}"
                                                           CornerRadius="4">
                                                                    <Grid>
                                                                        <Grid.ColumnDefinitions>
                                                                            <ColumnDefinition/>
                                                                            <ColumnDefinition Width="32"/>
                                                                        </Grid.ColumnDefinitions>

                                                                        <ContentPresenter Grid.Column="0"
                                                                            HorizontalAlignment="Left"
                                                                            VerticalAlignment="Center"
                                                                            Margin="10,0,0,0"/>

                                                                        <Path Grid.Column="1"
                                                                 Data="M 0 0 L 4 4 L 8 0 Z"
                                                                 Fill="{StaticResource TextBrush}"
                                                                 HorizontalAlignment="Center"
                                                                 VerticalAlignment="Center"/>
                                                                    </Grid>
                                                                </Border>
                                                                <ControlTemplate.Triggers>
                                                                    <Trigger Property="IsMouseOver" Value="True">
                                                                        <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                                                    </Trigger>
                                                                    <Trigger Property="IsChecked" Value="True">
                                                                        <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                                                        <Setter TargetName="Border" Property="BorderThickness" Value="2"/>
                                                                    </Trigger>
                                                                </ControlTemplate.Triggers>
                                                            </ControlTemplate>
                                                        </ToggleButton.Template>
                                                    </ToggleButton>

                                                    <ContentPresenter x:Name="ContentSite"
                                                         IsHitTestVisible="False"
                                                         Content="{TemplateBinding SelectionBoxItem}"
                                                         ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                         ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                         Margin="10,0,32,0"
                                                         VerticalAlignment="Center"
                                                         HorizontalAlignment="Left"/>

                                                    <Popup x:Name="Popup"
                                              Placement="Bottom"
                                              IsOpen="{TemplateBinding IsDropDownOpen}"
                                              AllowsTransparency="True"
                                              Focusable="False"
                                              PopupAnimation="Slide">
                                                        <Grid x:Name="DropDown"
                                                 SnapsToDevicePixels="True"
                                                 MinWidth="{TemplateBinding ActualWidth}"
                                                 MaxHeight="300">
                                                            <Border x:Name="DropDownBorder"
                                                       Background="White"
                                                       BorderBrush="{StaticResource BorderBrush}"
                                                       BorderThickness="1"
                                                       CornerRadius="4"
                                                       Margin="0,4,0,0">
                                                                <Border.Effect>
                                                                    <DropShadowEffect Color="#000000" Direction="270" ShadowDepth="2" BlurRadius="8" Opacity="0.15"/>
                                                                </Border.Effect>

                                                                <ScrollViewer Margin="4,6"
                                                                 SnapsToDevicePixels="True">
                                                                    <StackPanel IsItemsHost="True"
                                                                   KeyboardNavigation.DirectionalNavigation="Contained"/>
                                                                </ScrollViewer>
                                                            </Border>
                                                        </Grid>
                                                    </Popup>
                                                </Grid>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ComboBox.Style>

                            <ComboBox.ItemContainerStyle>
                                <Style TargetType="ComboBoxItem">
                                    <Setter Property="Padding" Value="10,8"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ComboBoxItem">
                                                <Border x:Name="Border"
                                           Background="Transparent"
                                           Padding="{TemplateBinding Padding}">
                                                    <StackPanel>
                                                        <TextBlock Text="{Binding Name}"
                                                      FontWeight="Medium"
                                                      FontSize="13"
                                                      Foreground="{StaticResource TextBrush}"/>
                                                        <TextBlock Text="{Binding Description}"
                                                      FontSize="11"
                                                      Foreground="{StaticResource SecondaryTextBrush}"
                                                      Margin="0,2,0,0"
                                                      TextWrapping="Wrap"/>
                                                    </StackPanel>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="Border" Property="Background" Value="{StaticResource HoverBrush}"/>
                                                    </Trigger>
                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter TargetName="Border" Property="Background" Value="{StaticResource SelectedBrush}"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ComboBox.ItemContainerStyle>
                        </ComboBox>
                    </StackPanel>

                    <!-- 中间标题 -->
                    <StackPanel Grid.Column="1" 
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center">
                        <TextBlock Text="{Binding CurrentScenarioTitle}" 
                      HorizontalAlignment="Center"
                      FontWeight="SemiBold" 
                      FontSize="16"
                      Foreground="{StaticResource TextBrush}"/>
                        <TextBlock Text="{Binding CurrentScenarioSubtitle}" 
                      HorizontalAlignment="Center"
                      FontSize="11"
                      Foreground="{StaticResource SecondaryTextBrush}"
                      Margin="0,2,0,0"/>
                    </StackPanel>

                    <!-- 工具按钮 -->
                    <StackPanel Grid.Column="2" 
                   Orientation="Horizontal" 
                   HorizontalAlignment="Right">

                        <Button ToolTip="{Binding RefreshTip}"
                   Width="36" Height="36"
                   Margin="4,0"
                   Command="{Binding RefreshChartsCommand}"
                   Style="{StaticResource BaseButtonStyle}"
                   Background="Transparent">
                            <Path Data="{StaticResource RefreshGeometry}" 
                     Fill="{StaticResource TextBrush}" 
                     Width="16" Height="16"
                     Stretch="Uniform"/>
                        </Button>

                        <Button ToolTip="{Binding ExportTip}"
                   Width="36" Height="36"
                   Margin="4,0"
                   Command="{Binding ExportDataCommand}"
                   Style="{StaticResource BaseButtonStyle}"
                   Background="Transparent">
                            <Path Data="{StaticResource ExportGeometry}" 
                     Fill="{StaticResource TextBrush}" 
                     Width="16" Height="16"
                     Stretch="Uniform"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- 双图表显示区域 -->
            <Grid Grid.Row="1" Margin="20,12,20,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="1.5*"/>
                </Grid.ColumnDefinitions>

                <!-- 工艺操作窗口图表 -->
                <Border Grid.Column="0" 
                       Background="White"
                       CornerRadius="8"
                       BorderBrush="{StaticResource BorderBrush}"
                       BorderThickness="1">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" 
                               Background="{StaticResource ControlBackgroundBrush}"
                               CornerRadius="8,8,0,0"
                               Padding="16,10">
                            <TextBlock Text="{Binding OpratorViewPort}" 
                                      FontSize="16" 
                                      FontWeight="SemiBold" 
                                      Foreground="{StaticResource TextBrush}"/>
                        </Border>

                        <oxy:PlotView Grid.Row="1"
                                     Model="{Binding ProcessWindowPlotModel}"
                                     Background="White" 
                                     Margin="12"/>
                    </Grid>
                </Border>

                <!-- 参数敏感性分析图表 -->
                <Border Grid.Column="2" 
                       Background="White"
                       CornerRadius="8"
                       BorderBrush="{StaticResource BorderBrush}"
                       BorderThickness="1">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" 
                               Background="{StaticResource ControlBackgroundBrush}"
                               CornerRadius="8,8,0,0"
                               Padding="12,8">
                            <TextBlock Text="{Binding ParamSensitivityAnalysis}" 
                                      FontSize="14" 
                                      FontWeight="SemiBold" 
                                      Foreground="{StaticResource TextBrush}"/>
                        </Border>

                        <oxy:PlotView Grid.Row="1"
                                     Model="{Binding SensitivityPlotModel}"
                                     Background="White" 
                                     Margin="8"/>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>